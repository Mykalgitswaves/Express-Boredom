{% extends 'base-layout.njk' %}

{% block content %}
<nav class="w-100 px-20 grid gap-x-4">
    <a class="blue-500 hover:blue-400" href="/sign-up">Sign up</a>

    <a class="blue-500 hover:blue-400" href="/learn-more">Learn more</a>
    {% if user %}
            <p>
                <b>{{ user.fullName }}</b>
                    <br/>{{ user.YOE }}
            </p>
        <a href="/logout">Logout</a>
    {% endif %}
</nav>

<main class="w-100">
        <div class="container grid place-content-center">
            <h1 class="">Bookmarked/BookClub</h1>

            <h3 class="text-subheading">Subheading marketing pitch for this thing</h3>

            <a 
              class="btn btn-cta purple"
              href="{{ mainBtnHref }}"
            >{{ mainBtnCta }}</a>
        </div>    
</main>
{% endblock %}